# Risk register

This register tracks threats to product substance, correctness, safety, and
adoption. A risk closes only when observed evidence addresses it.

Status values:

- `open`: material and not yet tested;
- `testing`: an active phase has an explicit experiment;
- `contained`: mitigated for the supported scope;
- `reframed`: evidence changed the product boundary;
- `accepted`: residual risk is explicit and owned.

## Active risks

| ID | Risk | Severity | Status | Current control | Evidence required |
|---|---|---:|---|---|---|
| R-01 | Catalog evidence is incomplete or stale, so readiness overstates coverage | critical | contained for current Core boundary | The benchmark achieved 100% recall in the isolated truth graph, directly reread aspects, categorized an injected page failure as `PARTIAL`, blocked stale native data despite fresh metadata, and reopened on a late consumer. MCP field lineage can qualify but never exclude members of the complete GMS universe | Production graph completeness and ingestion latency remain deployment-specific |
| R-02 | A DataHub entity cannot be mapped reliably to the exact native object | critical | contained for the supported Git/dbt boundary | Phase 03 required one fresh DataHub consumer URN to equal one explicit dbt manifest metadata URN. The benchmark matched the exact controlled identity and refused ambiguous identity while table-only and non-Git entities remained opaque. Zero or multiple mappings refuse | Production identity collisions and renamed native objects remain deployment-specific |
| R-03 | Native validation proves syntax but not business equivalence | critical | contained | The clean migration passed dbt parse, seed, build, and seven native tests. The benchmark exposed that consumer-to-replacement equivalence alone could accept a faulty replacement, added source-level legacy/replacement parity, and then observed exact null, semantic-drift, and unmapped-category failures | Each customer project still needs domain-meaningful source parity and consumer tests |
| R-04 | New consumers appear after inventory and before producer change | critical | contained for supported path | A controlled live degree-two consumer changed the verified campaign from `READY_TO_RETIRE` to `UNSAFE`; the later gate refused and sentinel count stayed one | Production ingestion latency and a consumer appearing inside a separately privileged warehouse transaction remain outside this fixture |
| R-05 | A source changes between plan and apply | critical | contained for Git/dbt | Live Git/dbt apply rereads branch, commit, clean status, target fingerprint, and attributes immediately before one-file apply | Benchmark branch movement, regenerated manifest, and source-fingerprint drift |
| R-06 | Partial native mutation leaves a consumer in a worse state | critical | contained for supported Git/dbt scope | One-file Git apply is atomic, interruption is recovered from repository state, compensation is a verified Git reversal, and outcome remains visible | Repeat interruption, compensation, and idempotent reapply after benchmark integration |
| R-07 | Overlapping campaigns mutate the same consumer inconsistently | high | contained for supported local boundary | Git/dbt apply acquires the exact native-identity claim only after approval. The campaign store binds one writer and path, takes an exclusive lock, refuses known shared filesystems, and rejects copied state before database change | Benchmark overlapping Git/dbt campaigns and unrecognized custom shared filesystems |
| R-08 | Adapter work grows linearly into bespoke consulting | critical | reframed | Looker was removed rather than allowing platform count to drive the product. Git/dbt is the sole executor; non-repository consumers remain blockers or externally receipted | A real operator requirement, not speculative breadth, must justify any future executor |
| R-09 | The product becomes a report or approval tracker rather than an executor | critical | contained for supported vertical | Two benchmark runs reused the campaign engine for live DataHub discovery, one exact Git/dbt commit, dbt-native validation, reconciliation, one verified publication, one producer sentinel, and late/rich refusal | Installed-product replay is rerun after deprecated-surface removal |
| R-10 | DataHub becomes decorative because repository tools find the same scope | critical | contained for the supported benchmark | DataHub-only graph evidence added late and opaque external consumers that repository analysis could not see, reversed a ready decision, and caused the producer gate to refuse | Production graph breadth and ingestion completeness remain deployment-specific |
| R-11 | DataHub Core and Cloud capability differences break portability | high | contained for pinned Core; Cloud untested | The benchmark passed on Core v1.6.0 using capability-selected GraphQL/MCP behavior and direct aspect reread without a Cloud-only dependency. DataHub Cloud remains optional and unclaimed | A Cloud deployment requires its own compatibility evidence |
| R-12 | DataHub summary storage is mistaken for transactional campaign state | high | contained | Four writes and read-backs updated one DataHub URN while SQLite remained authoritative and publication receipts never drove policy. Phase 08 observed delayed agent-visible read-back; the runner now preserves transient refusals and polls only the read after one write | External publication latency, write failure, and final publication must remain resumable from local state |
| R-13 | Digests are presented as proof of authorship | high | contained | Operator views label the value as a manifest integrity digest, retain evidence mode separately, and state that reports are not authorization; the gate verifies trusted run/principal provenance independently | Deployed native phases must continue to establish provenance outside the digest |
| R-14 | Apply credentials are too broad or leak through evidence | critical | contained for supported local scope | Git/dbt apply is opt-in, branch/path bound, and separate from the producer action. Deployment preflight reports only reference names; metrics have no remote export; secret and public-artifact reviews pass | Re-run scans after dataset acquisition and deprecated-surface removal |
| R-15 | Query history or raw evidence exposes sensitive SQL or data | high | contained for tracked boundary | Query-bearing evidence stays redacted and ignored. Eight benchmark artifacts contain aggregates and digests but no source rows, exact planted row IDs, cache paths, private host paths, credentials, or raw native output; both public and secret scans passed | Production evidence still requires source-specific redaction review |
| R-16 | Waivers either deadlock campaigns or become a quiet bypass | high | contained | Distinct digest-bound waiver records require authority, scope, reason, residual risk, and expiry; phase 05 carries and renders review reasons separately from blockers while default policy still blocks | A live waived campaign and producer gate must retain the same distinction |
| R-17 | A disappearing lineage edge is treated as proof of migration | critical | contained for the benchmark | Closure requires a valid native receipt, verified removal, or proved non-applicability. The oracle matched disappearing-edge refusal, and live table-only lineage remained opaque rather than becoming closure | Production graph deletion and ingestion lag remain deployment-specific |
| R-18 | Native retries duplicate or broaden mutations | high | contained for Git/dbt | Repeated Git/dbt apply retained one commit and exact target equality; interruption recovery rereads Git state before reapply | Repeat across generated clean and drift scenarios |
| R-19 | Validation queries create excessive cost or side effects | high | contained for current local data plane | dbt validation uses copied projects and local DuckDB/SQLite with no production warehouse or paid service | Bound row counts, memory, time, cache size, and query retention for medium/full benchmark tiers |
| R-20 | Final producer action bypasses the gate | critical | contained for representative workflow | The trusted producer command wrote one sentinel only for the live ready manifest; late, rich, unavailable, drifted, tampered, and untrusted paths all exited non-zero without another action | A real warehouse migration workflow must call the same gate immediately before its separately privileged change |
| R-21 | Customers perform retirements too rarely to support ongoing adoption | high | open | The independent evaluation protocol now records a recent baseline, frequency, time, steps, handoffs, interventions, willingness, rejection, and buyer role without sensitive details | One real prospective operator observation must show recurring use and a budget owner; the author-run reference is not evidence |
| R-22 | A close competitor closes the documented cross-system execution gap | high | open | Maintain qualified capability matrix; win on open protocol and DataHub fit | Current primary-source review before major positioning changes |
| R-23 | The system cannot resume deterministically after interruption | high | contained for supported local runtime | Append-only events, prefix-safe cache repair, and injected crashes reproduce one manifest without duplicates. Verified mode-`0600` online backup and original-path restore preserve integrity, foreign keys, event replay, gate ledger, canonical manifest, logical snapshot, and schema migrations 1 through 3 | Rehearse benchmark acquisition/generation interruption plus package upgrade and original-path restore after removal |
| R-24 | Generated changes are overbroad, templated, or miss indirect references | high | contained | Discovery records bounded text, dbt manifest, and compiled SQL separately; alias, macro/generated, and `SELECT *` uncertainty remains visible, while apply changes only one explicit manifest-mapped file | Broader generated-code patterns remain outside the supported repository shape |
| R-25 | Source deletion or branch movement invalidates the evidence chain | high | contained for Git path | Source commit, branch, file, and producer marker are pinned; consumer-file and producer-source drift both refused at gate time without another sentinel | Missing repository, deleted target, and force-move recovery remain phase 07 cases |
| R-26 | The campaign state machine can remain blocked forever | medium | contained for operator path | Every blocker and review condition now names a stable code, evidence source, and safe recovery action in CLI and HTML; resume requires an explicit legal state and idempotency key | Real unresolved cases must demonstrate accountable recovery without being mislabeled or bypassed |
| R-27 | Clock skew or an untrusted wall clock makes freshness and expiration decisions incorrect | critical | testing | Injected clock fixtures refuse rollback, invalid/naive time, future evidence, boundary expiry, and excessive skew | Deployment must establish a trusted clock and live source freshness |
| R-28 | A repository, dbt project, dependency, macro, or hook executes untrusted code outside the validation scope | critical | contained | Bubblewrap runs a copied tree with unshared network, no host home or source mount, an allowlisted environment, and resource limits; symlink, dependency, path, secret-read, subprocess, network, and Git-hook probes were refused or contained | Evidence is specific to the observed Linux/bubblewrap boundary and public fixture; other kernels require equivalent isolation proof |
| R-29 | A deleted and recreated native object is mistaken for the previously approved identity | critical | contained for Git/dbt; testing in benchmark | The Git plan binds repository, branch, commit, manifest node, path, graph URN, and content fingerprint; a renamed or recreated node cannot inherit approval | Exercise delete/recreate, renamed manifest node, and recycled DataHub display-name scenarios |
| R-30 | The replacement field or its compatibility changes after validation but before the producer gate | critical | contained for Git/dbt path | Direct DataHub reread refused `TEXT` to `INTEGER` drift; native dbt rejected unmapped categories, null inflation, and semantic divergence; the late graph change reversed readiness and the gate refused without another sentinel | A separately privileged real producer transaction retains an unavoidable external race |
| R-31 | A timeout or cancellation after apply leaves mutation outcome unknown and a retry duplicates or widens the change | critical | contained for current Git/dbt scope | Git state and one-file commit identity are reread after interruption before any retry; no remote non-transactional native mutation remains supported | Repeat hard interruption at plan, apply, validation, compensation, reconciliation, and gate boundaries |
| R-32 | Table-level lineage is presented as proof of a field-level dependency or closure | critical | contained for the current path | Direct Core reread distinguished exact field lineage from a table-only external edge; the latter stayed opaque and `UNSAFE` and could not authorize mutation or closure | Other DataHub versions require equivalent granularity evidence |
| R-33 | Policy, configuration, validator, or authorization changes between validation and gate | critical | contained for Git/dbt; testing in benchmark | Exact input digests and native versions are rebound into the Git plan, receipt, reconciliation, publication, producer plan, and gate | Exercise registry, generator, oracle, graph, dbt version, policy, authorization, and configuration drift |
| R-34 | The producer gate trusts an unproven artifact or fails open when campaign state is unavailable | critical | contained for supported local gate | Wrong run/writer, missing store, unavailable DataHub, tampered validation, failed publication, and untrusted context all refuse; content digests are distinct from provenance | Production CI identity and secret-reference deployment remain phase 07/08 evidence |
| R-35 | A green gate result is replayed or state changes between the check and the producer action | critical | contained for supported local gate | One exact plan is durably issued per canonical manifest; intent precedes action; replay, unissued alternate plan, expiry, source drift, and outcome-unknown cases consume or refuse safely | A real producer transaction still has an unavoidable external race after the local sentinel boundary |
| R-36 | Two runners use divergent local campaign stores and each believes it is authoritative | critical | contained for supported local deployment | Store metadata binds one writer to one resolved path, an exclusive local lock prevents simultaneous writers, known shared filesystems refuse, and the installed Phase 08 preflight exposed the binding across four clean runtimes. A byte-valid copied store refused before lock, journal, or database change | Unrecognized custom filesystems and real operator/CI adherence to the single-writer runbook remain |
| R-37 | Connector success or empty-result counters disagree with stored metadata | high | contained for the benchmark | Ingestion completion never authorizes evidence by itself; exact schema, lineage, ownership, quality, and publication aspects were directly reread from DataHub, while partial paging and replacement contradictions refused | New connectors and production ingestion reports require equivalent direct reread |
| R-38 | The synthetic oracle shares implementation with the campaign policy and merely confirms itself | critical | contained for the benchmark | The independent module does not import policy; all 14 expected decisions and stable refusal sets matched the real policy boundary in three full runs, zero false readiness occurred, and an intentionally corrupted expectation was rejected | New scenarios must retain the same implementation separation |
| R-39 | Moving, unlicensed, oversized, or tampered public datasets make evidence irreproducible or unsafe | high | contained for current inputs | Four official assets are pinned by full revision, reviewed license evidence, size, and SHA-256; offline verification, two byte-identical generations, three semantically identical live runs, package exclusion, and row-free public evidence all passed | Any upstream revision or license change requires a new reviewed registry and evidence chain |
| R-40 | Official dataset prose differs from the exact pinned bytes and causes the benchmark to encode an imagined fault | high | contained for current probes | Commit `ef02788` queries the read-only native databases and records aggregate observations; the nyc-taxi oracle uses the observed nine-day gap and absence of a zero-trip row rather than the README's three-day/empty-load claim | Preserve source revision and probe digest in live evidence; refuse if future bytes or claims are substituted without review |

## Product-pressure interpretation

### Highest substance risk

R-09 and R-38 decide whether Retirement Conductor remains a product rather
than a report or self-confirming demo. The benchmark must exercise the real
mutation/reconciliation/gate engine against an independent oracle.

### Highest safety risks

R-01, R-02, R-04, R-06, R-14, R-17, R-20, and R-27 through R-36 can produce
a false `READY_TO_RETIRE`. They take priority over breadth and visual polish.

### Highest adoption risk

R-21 is not solved by more code. The complete vertical can prove capability,
but only real users can prove frequency, willingness to adopt, and budget.

## Review rule

Every phase document names the risks it changes. When evidence is added:

1. link the artifact or test;
2. explain what the result proves and does not prove;
3. update status;
4. add a new risk when the result exposes one;
5. never close a broader risk with a narrower fixture.
