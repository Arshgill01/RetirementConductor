# Risk register

This register tracks threats to product substance, correctness, safety, and
adoption. A risk closes only when observed evidence addresses it.

Status values:

- `open`: material and not yet tested;
- `testing`: an active phase has an explicit experiment;
- `contained`: mitigated for the supported scope;
- `reframed`: evidence changed the product boundary;
- `accepted`: residual risk is explicit and owned.

## Active risks

| ID | Risk | Severity | Status | Current control | Evidence required |
|---|---|---:|---|---|---|
| R-01 | Catalog evidence is incomplete or stale, so readiness overstates coverage | critical | contained for current Core boundary | Phase 08 reproduced a stale zero-result lineage cache while the new direct edge existed. Complete GMS paging now requests `skipCache`, retains request and response evidence, and becomes partial if cache use is reported; the cache-bypassed query observed the exact late consumer and base restoration removed it. MCP field lineage can only qualify, never exclude, members of that complete GMS universe. Phase 06 also directly rereads stored LookML aspects instead of trusting contradictory connector counters | Core omits cache freshness metadata, so authenticated or Cloud cache observability and real saved-Look ingestion-retention behavior remain |
| R-02 | A DataHub entity cannot be mapped reliably to the exact native object | critical | testing | Phase 03 required one fresh DataHub consumer URN to equal one explicit dbt manifest metadata URN. The deterministic Looker path additionally binds one exact graph URN and snapshot digest to project, model, Explore, folder, numeric Look ID, content-metadata ID, and creation time; zero, multiple, or recreated matches refuse | Live Looker search, ingestion, and native identity evidence remain |
| R-03 | Native validation proves syntax but not business equivalence | critical | contained | The supported dbt project runs an explicit semantic-equivalence data test; the compatible change passed while an `order_amount` substitution failed native build | Each later adapter and customer project still needs a meaningful source-native semantic validator |
| R-04 | New consumers appear after inventory and before producer change | critical | contained for supported path | A controlled live degree-two consumer changed the verified campaign from `READY_TO_RETIRE` to `UNSAFE`; the later gate refused and sentinel count stayed one | Production ingestion latency and a consumer appearing inside a separately privileged warehouse transaction remain outside this fixture |
| R-05 | A source changes between plan and apply | critical | contained for Git; testing for Looker | Live Git/dbt apply rereads branch, commit, clean status, target fingerprint, and attributes. The deterministic Looker adapter rereads immutable identity, query, schedules, folder target set, compatibility, and content fingerprint immediately before apply | Live Looker stale-source and schedule-drift refusals remain |
| R-06 | Partial native mutation leaves a consumer in a worse state | critical | testing | One-file Git apply is atomic. Phase 07 injected timeout, connection loss, 408, 425, 500, invalid JSON, lost response, interruption, and compensation conflict; every ambiguous Looker mutation made one request, remained `APPLY_OUTCOME_UNKNOWN`, and required native reread instead of blind retry | Live Looker loss, cancellation, partial mutation, and compensation remain |
| R-07 | Overlapping campaigns mutate the same consumer inconsistently | high | contained for supported local boundary | Both native workflows acquire the same exact native-identity claim only after approval. The campaign store also binds one writer and one resolved path, takes an exclusive local lock, refuses known shared filesystems, and rejects copied state before database change | Live concurrent Looker attempts and unrecognized custom shared filesystems remain unobserved |
| R-08 | Adapter work grows linearly into bespoke consulting | critical | testing | The second adapter reuses campaign approval, native claim, durable state, receipt, reconciliation, policy, and operator contracts while retaining documented Looker-specific identity, mutation, and validation code | A complete live Looker run must show that the shared contract reduces real integration effort |
| R-09 | The product becomes a report or approval tracker rather than an executor | critical | contained for first vertical; testing for second | One live campaign completed discovery through producer action. The deterministic Looker workflow now performs an exact POST-query/PATCH-Look mutation, native validation, compensation, and reconciliation through the same campaign rather than stopping at a plan | Live Looker execution must demonstrate the heterogeneous loop |
| R-10 | DataHub becomes decorative because repository tools find the same scope | critical | contained for first product graph | Phase 04 repeated the 31-consumer rich refusal and used DataHub-only graph change to add a late consumer and reverse a ready producer decision | Repeat with live Looker and a non-synthetic customer topology |
| R-11 | DataHub Core and Cloud capability differences break portability | high | testing | Live Core capability fingerprint selects GraphQL fallbacks for pagination and document read-back and records unavailable or conditional MCP surfaces. DataHub 1.6.0 LookML reporting contradicted its stored aspects, so ingestion acceptance now requires direct reread. The Phase 08 compatibility matrix labels Core as executed and Cloud as optional and not run | Exercise an authenticated or Cloud boundary and a newer connector without assuming the Core tool union or report semantics |
| R-12 | DataHub summary storage is mistaken for transactional campaign state | high | contained | Four writes and read-backs updated one DataHub URN while SQLite remained authoritative and publication receipts never drove policy. Phase 08 observed delayed agent-visible read-back; the runner now preserves transient refusals and polls only the read after one write | External publication latency, write failure, and final publication must remain resumable from local state |
| R-13 | Digests are presented as proof of authorship | high | contained | Operator views label the value as a manifest integrity digest, retain evidence mode separately, and state that reports are not authorization; the gate verifies trusted run/principal provenance independently | Deployed native phases must continue to establish provenance outside the digest |
| R-14 | Apply credentials are too broad or leak through evidence | critical | testing | Apply is opt-in and target-bound. Looker plan permissions exclude `save_content`; the plan-only receipt performed no PATCH or query creation, while apply-enabled preflight requires it. Installed deployment preflight reports only missing reference names, local metrics have no remote export, and the final 294-file secret plus 53-file public-artifact reviews passed | Effective least-privilege permissions and deployed secret-provider storage still require live Looker evidence |
| R-15 | Query history or raw evidence exposes sensitive SQL or data | high | contained for tracked and local artifact boundary; testing externally | Query-bearing evidence is redacted before ignored storage; public export structurally replaces identities and sensitive text; security policy now fixes raw-evidence, database, backup, log, deletion, and caveat handling; injected values stayed out of public output and both 270-file secret and 46-file artifact reviews passed | Non-empty live Looker query evidence, host backup encryption, and deployed retention enforcement remain |
| R-16 | Waivers either deadlock campaigns or become a quiet bypass | high | contained | Distinct digest-bound waiver records require authority, scope, reason, residual risk, and expiry; phase 05 carries and renders review reasons separately from blockers while default policy still blocks | A live waived campaign and producer gate must retain the same distinction |
| R-17 | A disappearing lineage edge is treated as proof of migration | critical | contained by deterministic policy | Closure requires native evidence. Looker reconciliation separately records exact-field, table-only, and absent states; disappearance is explicitly false as closure, a remaining exact legacy edge stales the receipt, and only native validation plus an exact replacement edge supports graph closure | Real Looker connector deletion/recreate and indexing semantics remain |
| R-18 | Native retries duplicate or broaden mutations | high | testing | Repeated Git/dbt apply retained one commit. Phase 07 bounded idempotent Looker reads across rate limit and transient failures, while six ambiguous mutation classes never retried; immutable-query reuse, one-property target equality, and lost-response reread retain one PATCH | Live Looker idempotency, service-side retries, and duplicate-query behavior remain |
| R-19 | Validation queries create excessive cost or side effects | high | testing | DuckDB validation is isolated. Looker execution is bounded to a configured row limit, disables cache, retains no result rows, and DataHub usage-history extraction is disabled; the pretrial boundary retains zero BigQuery query quota | A live safe warehouse connection still needs cost, cancellation, and query-side-effect evidence |
| R-20 | Final producer action bypasses the gate | critical | contained for representative workflow | The trusted producer command wrote one sentinel only for the live ready manifest; late, rich, unavailable, drifted, tampered, and untrusted paths all exited non-zero without another action | A real warehouse migration workflow must call the same gate immediately before its separately privileged change |
| R-21 | Customers perform retirements too rarely to support ongoing adoption | high | open | The independent evaluation protocol now records a recent baseline, frequency, time, steps, handoffs, interventions, willingness, rejection, and buyer role without sensitive details | One real prospective operator observation must show recurring use and a budget owner; the author-run reference is not evidence |
| R-22 | A close competitor closes the documented cross-system execution gap | high | open | Maintain qualified capability matrix; win on open protocol and DataHub fit | Current primary-source review before major positioning changes |
| R-23 | The system cannot resume deterministically after interruption | high | contained for supported local runtime | Append-only events, prefix-safe cache repair, and injected crashes reproduce one manifest without duplicates. Verified mode-`0600` online backup and original-path restore preserve integrity, foreign keys, event replay, gate ledger, canonical manifest, logical snapshot, and schema migrations 1 through 3. The installed 0.1.0-to-0.2.0 rehearsal preserved the campaign and restored the prior package/database pair | Live Looker outcome recovery, encrypted off-host backup, and operator-specific restore remain |
| R-24 | Generated changes are overbroad, templated, or miss indirect references | high | contained | Discovery records bounded text, dbt manifest, and compiled SQL separately; alias, macro/generated, and `SELECT *` uncertainty remains visible, while apply changes only one explicit manifest-mapped file | Broader generated-code patterns remain outside the supported repository shape |
| R-25 | Source deletion or branch movement invalidates the evidence chain | high | contained for Git path | Source commit, branch, file, and producer marker are pinned; consumer-file and producer-source drift both refused at gate time without another sentinel | Missing repository, deleted target, and force-move recovery remain phase 07 cases |
| R-26 | The campaign state machine can remain blocked forever | medium | contained for operator path | Every blocker and review condition now names a stable code, evidence source, and safe recovery action in CLI and HTML; resume requires an explicit legal state and idempotency key | Real unresolved cases must demonstrate accountable recovery without being mislabeled or bypassed |
| R-27 | Clock skew or an untrusted wall clock makes freshness and expiration decisions incorrect | critical | testing | Injected clock fixtures refuse rollback, invalid/naive time, future evidence, boundary expiry, and excessive skew | Deployment must establish a trusted clock and live source freshness |
| R-28 | A repository, dbt project, dependency, macro, or hook executes untrusted code outside the validation scope | critical | contained | Bubblewrap runs a copied tree with unshared network, no host home or source mount, an allowlisted environment, and resource limits; symlink, dependency, path, secret-read, subprocess, network, and Git-hook probes were refused or contained | Evidence is specific to the observed Linux/bubblewrap boundary and public fixture; other kernels require equivalent isolation proof |
| R-29 | A deleted and recreated native object is mistaken for the previously approved identity | critical | testing | The Looker plan binds numeric content ID, content-metadata ID, creation time, graph URN, query, and content fingerprint. Deterministic delete/recreate simulation rejects apply and reconciliation with `IDENTITY_NATIVE_OBJECT_RECREATED` and stales only its receipt | Live delete/recreate ingestion and recycled-ID behavior remain |
| R-30 | The replacement field or its compatibility changes after validation but before the producer gate | critical | contained for Git/dbt path | The gate reread the live field pair and refused an injected `VARCHAR` to `INTEGER` replacement drift with `SPEC_REPLACEMENT_INCOMPATIBLE` | Looker and production warehouse schema-version behavior remain |
| R-31 | A timeout or cancellation after apply leaves mutation outcome unknown and a retry duplicates or widens the change | critical | testing | Looker intent is durable as `PENDING`, `OUTCOME_UNKNOWN`, or `RESOLVED`; timeout, connection loss, 408, 425, 500, invalid JSON, dropped-response, and hard-interruption fixtures each make one mutation attempt and recover only by native reread, while unmatched state remains unknown | Repeat timeout, cancellation, rate-limit, and lost-response recovery against the live API |
| R-32 | Table-level lineage is presented as proof of a field-level dependency or closure | critical | contained for current adapters | Live DataHub table-only edges retain a limitation. Looker reconciliation accepts only an exact claim object with column-lineage confidence as a field edge; table-only replacement evidence emits `NOT_CLAIMED_CONNECTOR_LIMITATION` | Confirm the distinction on live Looker ingestion |
| R-33 | Policy, configuration, validator, or authorization changes between validation and gate | critical | contained for Git/dbt; testing for Looker | Exact input digests and native versions are rebound into the Git plan. Looker binds graph snapshot, principal capabilities, immutable object identity, source/query/schedule fingerprints, replacement evidence, target set, plan, apply, and receipt across reconciliation | Deployment rollout and a live combined gate still need Looker drift evidence |
| R-34 | The producer gate trusts an unproven artifact or fails open when campaign state is unavailable | critical | contained for supported local gate | Wrong run/writer, missing store, unavailable DataHub, tampered validation, failed publication, and untrusted context all refuse; content digests are distinct from provenance | Production CI identity and secret-reference deployment remain phase 07/08 evidence |
| R-35 | A green gate result is replayed or state changes between the check and the producer action | critical | contained for supported local gate | One exact plan is durably issued per canonical manifest; intent precedes action; replay, unissued alternate plan, expiry, source drift, and outcome-unknown cases consume or refuse safely | A real producer transaction still has an unavoidable external race after the local sentinel boundary |
| R-36 | Two runners use divergent local campaign stores and each believes it is authoritative | critical | contained for supported local deployment | Store metadata binds one writer to one resolved path, an exclusive local lock prevents simultaneous writers, known shared filesystems refuse, and the installed Phase 08 preflight exposed the binding across four clean runtimes. A byte-valid copied store refused before lock, journal, or database change | Unrecognized custom filesystems and real operator/CI adherence to the single-writer runbook remain |
| R-37 | Connector success or empty-result counters disagree with stored metadata | high | testing | The LookML recipe waits synchronously, requires positive source discovery with no drops or failures, and promotes evidence only after rereading the exact stored schema and field lineage. Contradictory DataHub 1.6.0 counters remain visible as limitations | Repeat against live saved-Look ingestion and a newer connector release; empty or contradictory reports must never authorize absence or closure |

## Product-pressure interpretation

### Highest substance risk

R-08 and R-09 decide whether Retirement Conductor is a product. If every
platform requires a different orchestrator, or if the work ends in a report,
the cross-system thesis fails.

### Highest safety risks

R-01, R-02, R-04, R-06, R-14, R-17, R-20, and R-27 through R-36 can produce
a false `READY_TO_RETIRE`. They take priority over breadth and visual polish.

### Highest adoption risk

R-21 is not solved by more code. The complete vertical can prove capability,
but only real users can prove frequency, willingness to adopt, and budget.

## Review rule

Every phase document names the risks it changes. When evidence is added:

1. link the artifact or test;
2. explain what the result proves and does not prove;
3. update status;
4. add a new risk when the result exposes one;
5. never close a broader risk with a narrower fixture.
