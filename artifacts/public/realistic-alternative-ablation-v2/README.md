# Realistic alternative ablation v2

This bundle records the frozen CP-03 foundation comparison among a static
point-in-time sign-off, a competent failure-closed fresh pre-action CI check,
and the Retirement Conductor campaign/lease/gate protocol.

The predeclared classifier returned `NO_MATERIAL_ADVANTAGE` with a `SIMPLIFY`
recommendation. Fresh CI and Retirement Conductor both produced zero unsafe
committed actions, zero clean-control false refusals, no stale-green reuse,
correct replay/interruption outcomes, and equal required audit-binding
coverage. The static arm committed eight unsafe actions after evidence or
authority drift.

Start with `index.json`, then inspect `report.json`, `matrix.json`, the exact
`baseline-capability.json`, and retained `failure-attempts.json`. Verify the
bundle with:

```bash
uv run python scripts/run_realistic_alternative_ablation_v2.py verify
```

This is executable fixture evidence. CP-03 owns no live ports, and the bundle
does not claim a live PostgreSQL, DataHub, Superset, Git/dbt, or downstream
workload run. CP-05 must replace the explicit CP-01, CP-02, and CP-04 adapter
placeholders and rerun the decisive cases without changing the frozen rule.
