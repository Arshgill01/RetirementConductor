# Definitive consequential run

This directory is the public-safe evidence package for CP-05. Run:

```bash
uv run python scripts/run_definitive_consequential.py verify
```

The verifier checks the self-digested index, every listed file digest and size,
the unchanged CP-03 frozen inputs, and the frozen classification. All native
actions were confined to disposable loopback services. No credentials, row
values, cookies, unrestricted query text, or private host paths belong here.
